<%- contentFor('content') %>
<!-- jika key error yang muncul di browser ditunjukkan sebagai not defined -->
<% if (typeof errors != 'undefined') { %>
    <div class="alert alert-danger" role="alert">
    <ul>
        <!-- lakukan perulangan dengan foreach untuk mencetak error message -->
        <% errors.forEach(err => { %>
            <li>
                <%= err.msg %> 
            </li>
            <% }) %>
        </ul>
    </div>  
<% } %> 
<form action="/contact/update" method="post">
    <!-- menggunakan ternary option untuk cek value dari oldName apakah menyimpan data yang sama dengan oldName atau -->
    <input type="hidden" name="oldName" value="<%= (cont.oldName) ? cont.oldName : cont.name %>">
    <div class="mb-3">
        <label for="exampleInputEmail1" class="form-label">Name</label>
        <input type="text" name="name" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" required value="<%= cont.name %>">
    </div>
    <div class="mb-3">
        <label for="exampleInputEmail1" class="form-label">Email address</label>
        <input type="email" name="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" value="<%= cont.email %>">
    </div>
    <div class="mb-3">
        <label for="exampleInputEmail1" class="form-label">Mobile Phone</label>
        <input type="tel" name="phone" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" required value="<%= cont.phone %>">
    </div>
    <div class="d-inline">
        <button type="submit" class="btn btn-danger me-2">Ubah data</button>
        <a href="/contact/<%= cont.name %>" class="btn btn-danger">Back to Detail</a>
    </div>
</form>